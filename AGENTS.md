# AGENTS.md guidelines

you are in an alpine linux container

- use `apk` to isntall new software
- use `sudo -u maybeuser touch file.txt` to create general files as normal user by default or to run programs
