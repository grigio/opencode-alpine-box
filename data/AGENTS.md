# AGENTS.md guidelines

you are in an alpine linux container

- use /data/workspace as default folder for files or projects
- at the first run exec FIX-perms-user.sh
- use `sudo apk` to install new software
- use `uv` to manage python dependencies
